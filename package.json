{
  "name": "apify-pypi-downloads",
  "version": "0.1.0",
  "private": true,
  "description": "Apify Actor — PyPI metadata + download stats as an MCP-style tool server for LLM agents. Tools: package_details, download_stats, top_packages, search_packages.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "test": "vitest run"
  },
  "engines": { "node": ">=22" },
  "dependencies": {
    "apify": "^3.4.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
